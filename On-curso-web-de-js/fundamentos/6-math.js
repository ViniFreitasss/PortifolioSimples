const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)

console.log(area.toFixed(2))

/**
 * Já que o javascript não possuí exponencial utiliza-se o Math que é um objeto que nos permite executar essa função
 * o comando Math.PI esta chamando o valor PI
 * o comando Math.pow de fato executa a exponeciação setando o parametro que será elevado (a base) e o expoente 
 */